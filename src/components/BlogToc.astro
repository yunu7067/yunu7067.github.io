---
import type { HTMLElement } from "node-html-parser";

export interface Props {
	contents: HTMLElement[]
}

const { contents } = Astro.props as Props;
---

<div class="bg-gray-200 dark:bg-gray-700 rounded-sm p-4 mb-8" aria-label="Table of Contents">
	<div class="text-xl mb-2 font-bold">목차</div>
	{ contents.map((el)=> (
	<div class={`toc mb-1 ${el.tagName.replace(`H`, `depth` )}`}>
		<a href={`#${el.id}`}>{el.textContent}</a>
	</div>
	)) }
</div>

<style>
	.toc.depth1 {}

	.toc.depth2 {
		padding-left: 2rem;
	}
</style>