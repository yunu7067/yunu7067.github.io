import {classes} from '$utils';

export const MediumButton: string = classes(
  'h-12 px-4 inline-flex flex-row items-center justify-center gap-2 rounded-md',
);

export const SmallButton: string = classes(
  'h-9 px-[0.6875rem] px-3 inline-flex flex-row items-center justify-center gap-2 rounded-md text-sm',
);
