---
import {Image} from '@astrojs/image/components';

export interface Props {
  src: string;
  alt: string;
}

const {src, alt} = Astro.props as Props;
---

<div class='w-fit shadow-sm'>
  <Image src={src} alt={alt} format='webp' width={200} height={112} />
</div>
